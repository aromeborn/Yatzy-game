
const topBonus = false

function checkIfTopBonus (upperRowSum) {
    if (sum >= 63) {topBonus = true};
    function getsBonus (upperRowSum) {
        upperRowBonus && topBonus.sum = 50 
        }
    return topBonus
}

export default checkIfTopBonus
